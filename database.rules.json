{
  "rules": {
    ".read": false,
    "ebookLeads": {
      ".read": false,
      "$leadId": {
        ".write": "!data.exists()",
        ".validate": "newData.hasChildren(['email', 'createdAt', 'source']) && newData.child('email').isString() && newData.child('email').val().length >= 5 && newData.child('email').val().length <= 254 && newData.child('createdAt').isString() && newData.child('source').val() === 'free-starter-guide'"
      }
    }
  }
}
